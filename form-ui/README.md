# form-ui

Modern React frontend for the Dynamic Versioned Form Management System.

## Setup

1. Install dependencies:
   ```bash
   npm install
   ```
2. Start dev server:
   ```bash
   npm run dev
   ```
3. Open: `http://localhost:5173`
4. Ensure backend is running at: `http://localhost:8080`

## Configuration

Optional environment variables:

- `VITE_API_BASE_URL` (default: `http://localhost:8080/api`)
- `VITE_API_USERNAME` (default: `admin`)
- `VITE_API_PASSWORD` (default: `admin`)
